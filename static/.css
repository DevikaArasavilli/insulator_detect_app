:root {
    --primary: #4361ee;
    --primary-dark: #3a0ca3;
    --secondary: #f72585;
    --success: #4cc9f0;
    --danger: #f72585;
    --light: #f8f9fa;
    --dark: #212529;
    --gray: #6c757d;
    --light-gray: #e9ecef;
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', sans-serif;
    scroll-behavior: smooth;
    background-color: var(--light);
    color: #333;
}

/* Container */
.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
}

/* Header */
header {
    background-color: var(--dark);
    padding: 1rem 0;
    position: sticky;
    top: 0;
    z-index: 1000;
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    display: flex;
    align-items: center;
    color: var(--primary);
}

.logo i {
    margin-right: 0.5rem;
    color: var(--success);
}

.logo h1 {
    font-family: 'Montserrat', sans-serif;
    font-weight: 700;
    font-size: 1.5rem;
}

nav ul {
    list-style: none;
    display: flex;
    gap: 20px;
}

nav ul li a {
    color: white;
    text-decoration: none;
    font-weight: 600;
}

nav ul li a:hover {
    color: var(--primary);
}

/* Hero Section */
.hero {
    padding: 3rem 0;
    text-align: center;
    background: url('https://thumbs.dreamstime.com/b/closeup-view-sunsetlit-power-line-insulator-stacks-detailed-look-electrical-transmission-infrastructure-witness-371970549.jpg') no-repeat center center / cover;
    position: relative;
    color: white;
    height: 40vh;
    display: flex;
    align-items: center;
    justify-content: center;
}

.hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.6);
    z-index: 1;
}

.hero-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
    margin: 0 auto;
}

.hero h2 {
    font-family: 'Montserrat', sans-serif;
    font-size: 2.5rem;
    margin-bottom: 1rem;
}

.hero p {
    font-size: 1.1rem;
    margin-bottom: 2rem;
    opacity: 0.9;
}

/* Upload Section */
.upload-section {
    background-color: white;
    border-radius: 10px;
    padding: 2rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08);
    margin: -50px auto 3rem auto;
    position: relative;
    z-index: 10;
    max-width: 1000px;
}

.section-title {
    text-align: center;
    margin-bottom: 2rem;
    color: var(--primary-dark);
    font-family: 'Montserrat', sans-serif;
    font-size: 1.8rem;
}

.upload-container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
    margin-bottom: 2rem;
}

@media (max-width: 768px) {
    .upload-container {
        grid-template-columns: 1fr;
    }
}

.upload-box {
    border: 2px dashed #ccc;
    padding: 20px;
    text-align: center;
    border-radius: 15px;
    position: relative;
    transition: 0.3s;
    cursor: pointer;
}

.upload-box:hover {
    background-color: #f9f9f9;
    border-color: #007bff;
}

.upload-box i {
    font-size: 2.5rem;
    color: var(--primary);
    margin-bottom: 1rem;
}

.upload-box h3 {
    margin-bottom: 0.5rem;
    color: var(--dark);
}

.upload-box p {
    color: var(--gray);
    font-size: 0.9rem;
}

.upload-box input[type="file"] {
    position: absolute;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    opacity: 0;
    cursor: pointer;
}

/* Button */
.btn {
    display: inline-block;
    background-color: var(--primary);
    color: white;
    padding: 12px 24px;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 500;
    border: none;
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 1rem;
    text-align: center;
}

.btn:hover {
    background-color: var(--primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(67, 97, 238, 0.3);
}

.btn-block {
    display: block;
    width: 100%;
}

/* Footer */
footer {
    background-color: var(--dark);
    color: white;
    padding: 2rem 0;
    text-align: center;
}

.footer-content {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.footer-logo {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 1rem;
}

.footer-logo i {
    font-size: 1.5rem;
    color: var(--success);
}

.footer-logo h2 {
    font-family: 'Montserrat', sans-serif;
    font-weight: 700;
}

.footer-links {
    display: flex;
    gap: 20px;
    margin-bottom: 1rem;
}

.footer-links a {
    color: white;
    text-decoration: none;
    transition: all 0.3s ease;
}

.footer-links a:hover {
    color: var(--success);
}

.footer-social {
    display: flex;
    gap: 15px;
    margin-bottom: 1rem;
}

.footer-social a {
    color: white;
    font-size: 1.2rem;
    transition: all 0.3s ease;
}

.footer-social a:hover {
    color: var(--success);
    transform: translateY(-3px);
}

.copyright {
    font-size: 0.9rem;
    opacity: 0.7;
}
